if (typeof dir === 'undefined') {
    let dir = window.myGlobalDir;
}

$(document).ready(function () {

    /*$(".ligne-prospect").click(function () {
        window.location.href = $(this).attr('data-link');
    });*/
});
